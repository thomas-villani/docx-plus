# `docx_plus.core.oxml`

Element-construction and XPath helpers. All XML construction in the
library goes through these — see [the invariants](../concepts/invariants.md#the-invariants),
invariant 2.

::: docx_plus.core.oxml
    options:
      members:
        - el
        - sub
        - xpath
        - remove
        - build_complex_field
        - build_bookmark
        - insert_before_first_anchor
        - ordered_insert
        - body_document_for
